name: Maintainer 249 tag delta

on:
  push:
    branches: [feat/249-upload-item-tags]
    paths:
      - .github/workflows/maintainer-249-tag-delta.yml

permissions:
  contents: write

jobs:
  patch-and-test:
    runs-on: [self-hosted, gooru]
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/249-upload-item-tags
          fetch-depth: 0
      - name: Apply tag reconciliation fix and tests
        run: python3 .github/maintainer-249-tag-delta.py
      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci
      - name: Run focused unit tests
        working-directory: frontend
        run: npm run test:unit -- src/lib/state/uploadItems.test.ts src/lib/state/uploadWorkflow.chunking.test.ts
      - name: Run frontend type checks
        working-directory: frontend
        run: npm run check
      - name: Commit validated patch and remove temporary machinery
        shell: bash
        run: |
          rm .github/workflows/maintainer-249-tag-delta.yml .github/maintainer-249-tag-delta.py
          git config user.name "chatgpt-codex-connector"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add frontend/src/lib/state/uploadWorkflow.svelte.ts frontend/src/lib/components/AuthenticatedApp.svelte frontend/src/lib/state/uploadItems.test.ts frontend/src/lib/state/uploadWorkflow.chunking.test.ts .github/workflows/maintainer-249-tag-delta.yml .github/maintainer-249-tag-delta.py
          git commit -m "fix(upload): reconcile queue tag edits by delta"
          git push origin HEAD:feat/249-upload-item-tags
