name: Maintainer managed file deletion

on:
  push:
    branches: [feat/21-delete-uploaded-files]

permissions:
  contents: write

jobs:
  apply-and-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/21-delete-uploaded-files
          fetch-depth: 0
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Apply implementation once
        shell: bash
        run: |
          python3 <<'PY'
          from pathlib import Path

          helper = Path('internal/serve/file_delete.go')
          if not helper.exists():
              helper.write_text(r'''package serve

import (
    "context"
    "errors"
    "fmt"
    "os"
    "path/filepath"
    "strings"

    "gooru.local/types"
)

var ErrFileNotManaged = errors.New("file is outside configured upload targets")

type stagedFileDeletion struct {
    originalPath string
    stagedPath   string
    stagingDir  string
}

func (s *Server) canDeleteFilePath(path string) bool {
    path = strings.TrimSpace(path)
    if path == "" {
        return false
    }
    filePath, err := filepath.Abs(path)
    if err != nil {
        return false
    }
    filePath = filepath.Clean(filePath)
    for _, target := range s.cfg.Uploads.Targets {
        root := strings.TrimSpace(target.Path)
        if root == "" {
            continue
        }
        rootPath, err := filepath.Abs(root)
        if err != nil {
            continue
        }
        rel, err := filepath.Rel(filepath.Clean(rootPath), filePath)
        if err != nil || rel == "." || filepath.IsAbs(rel) || rel == ".." || strings.HasPrefix(rel, ".."+string(filepath.Separator)) {
            continue
        }
        return true
    }
    return false
}

func (s *Server) deleteManagedFile(ctx context.Context, publicID string) (bool, error) {
    file, err := s.getFileByPublicID(ctx, publicID)
    if err != nil {
        return false, err
    }
    if !s.canDeleteFilePath(file.Path) {
        return false, ErrFileNotManaged
    }

    staged, err := stageFileDeletion(file)
    if err != nil {
        return false, err
    }

    deleted, deleteErr := s.deleteFileByPublicID(ctx, publicID)
    if deleteErr != nil || !deleted {
        if staged != nil {
            if rollbackErr := staged.rollback(); rollbackErr != nil {
                return false, fmt.Errorf("delete library location: %v; restore file: %w", deleteErr, rollbackErr)
            }
        }
        if deleteErr != nil {
            return false, deleteErr
        }
        return false, ErrNotFound
    }

    if staged != nil {
        if err := staged.commit(); err != nil {
            if rollbackErr := staged.rollback(); rollbackErr != nil {
                return false, fmt.Errorf("remove staged file: %v; restore file: %w", err, rollbackErr)
            }
            return false, fmt.Errorf("remove staged file: %w", err)
        }
    }
    return true, nil
}

func stageFileDeletion(file types.FileInfo) (*stagedFileDeletion, error) {
    stagingDir, err := os.MkdirTemp(filepath.Dir(file.Path), ".gooru-delete-")
    if err != nil {
        if errors.Is(err, os.ErrNotExist) {
            return nil, nil
        }
        return nil, fmt.Errorf("prepare file deletion: %w", err)
    }
    staged := &stagedFileDeletion{
        originalPath: file.Path,
        stagedPath:   filepath.Join(stagingDir, filepath.Base(file.Path)),
        stagingDir:  stagingDir,
    }
    if err := os.Rename(file.Path, staged.stagedPath); err != nil {
        _ = os.Remove(stagingDir)
        if errors.Is(err, os.ErrNotExist) {
            return nil, nil
        }
        return nil, fmt.Errorf("stage file deletion: %w", err)
    }
    return staged, nil
}

func (d *stagedFileDeletion) rollback() error {
    if err := os.Rename(d.stagedPath, d.originalPath); err != nil {
        return err
    }
    _ = os.Remove(d.stagingDir)
    return nil
}

func (d *stagedFileDeletion) commit() error {
    if err := os.Remove(d.stagedPath); err != nil {
        return err
    }
    _ = os.Remove(d.stagingDir)
    return nil
}
''')

              Path('internal/serve/file_delete_test.go').write_text(r'''package serve

import (
    "bytes"
    "context"
    "errors"
    "net/http"
    "net/http/httptest"
    "os"
    "path/filepath"
    "testing"

    "gooru.local/types"
)

func TestDeleteModeRemovesManagedUploadFileAndLocation(t *testing.T) {
    server, cleanup := newTestBrowseServer(t)
    defer cleanup()

    page := listTestFiles(t, server, "kind:image", 1)
    file, err := server.getFileByPublicID(context.Background(), page.Files[0].ID)
    if err != nil {
        t.Fatalf("resolve file: %v", err)
    }
    server.cfg.Uploads.Targets = []UploadTarget{{ID: "managed", Name: "Managed", Path: filepath.Dir(file.Path)}}

    refreshed := listTestFiles(t, server, "kind:image", 1)
    if !refreshed.Files[0].CanDelete {
        t.Fatal("expected file under configured upload target to advertise physical deletion")
    }

    req := httptest.NewRequest(http.MethodDelete, "/api/v1/files/"+page.Files[0].ID, bytes.NewBufferString(`{"mode":"delete"}`))
    req.Header.Set("Content-Type", "application/json")
    rec := httptest.NewRecorder()
    server.Handler().ServeHTTP(rec, req)
    if rec.Code != http.StatusOK {
        t.Fatalf("expected delete 200, got %d: %s", rec.Code, rec.Body.String())
    }
    if _, err := os.Stat(file.Path); !errors.Is(err, os.ErrNotExist) {
        t.Fatalf("expected managed file to be removed from disk, stat err=%v", err)
    }
    missing := httptest.NewRecorder()
    server.Handler().ServeHTTP(missing, authedRequest(http.MethodGet, "/api/v1/files/"+page.Files[0].ID))
    if missing.Code != http.StatusNotFound {
        t.Fatalf("expected deleted location 404, got %d: %s", missing.Code, missing.Body.String())
    }
}

func TestDeleteModeRejectsFileOutsideUploadTargets(t *testing.T) {
    server, cleanup := newTestBrowseServer(t)
    defer cleanup()

    page := listTestFiles(t, server, "kind:image", 1)
    file, err := server.getFileByPublicID(context.Background(), page.Files[0].ID)
    if err != nil {
        t.Fatalf("resolve file: %v", err)
    }
    server.cfg.Uploads.Targets = []UploadTarget{{ID: "other", Name: "Other", Path: t.TempDir()}}

    req := httptest.NewRequest(http.MethodDelete, "/api/v1/files/"+page.Files[0].ID, bytes.NewBufferString(`{"mode":"delete"}`))
    req.Header.Set("Content-Type", "application/json")
    rec := httptest.NewRecorder()
    server.Handler().ServeHTTP(rec, req)
    assertAPIError(t, rec, http.StatusConflict, "file_not_managed")
    if _, err := os.Stat(file.Path); err != nil {
        t.Fatalf("unmanaged file should remain on disk: %v", err)
    }
    detail := httptest.NewRecorder()
    server.Handler().ServeHTTP(detail, authedRequest(http.MethodGet, "/api/v1/files/"+page.Files[0].ID))
    if detail.Code != http.StatusOK {
        t.Fatalf("unmanaged file should remain tracked, got %d: %s", detail.Code, detail.Body.String())
    }
}

func TestManagedDeleteRollsBackFileWhenLibraryMutationFails(t *testing.T) {
    dir := t.TempDir()
    path := filepath.Join(dir, "kept.jpg")
    if err := os.WriteFile(path, []byte("keep me"), 0600); err != nil {
        t.Fatal(err)
    }
    library := failingPhysicalDeleteLibrary{file: types.FileInfo{ID: 1, PublicID: "public", Path: path}}
    cfg := DefaultConfig(filepath.Join(dir, "gooru.db"))
    cfg.Auth.Enabled = false
    cfg.Uploads.Targets = []UploadTarget{{ID: "managed", Name: "Managed", Path: dir}}
    server := NewServerWithLibrary(cfg, library)

    if _, err := server.deleteManagedFile(context.Background(), "public"); err == nil {
        t.Fatal("expected library deletion failure")
    }
    body, err := os.ReadFile(path)
    if err != nil || string(body) != "keep me" {
        t.Fatalf("file was not restored after library failure: body=%q err=%v", body, err)
    }
}

type failingPhysicalDeleteLibrary struct {
    file types.FileInfo
}

func (l failingPhysicalDeleteLibrary) ListFiles(context.Context, string) ([]types.FileInfo, error) { return []types.FileInfo{l.file}, nil }
func (l failingPhysicalDeleteLibrary) GetFile(context.Context, int64) (types.FileInfo, error) { return l.file, nil }
func (l failingPhysicalDeleteLibrary) ListTags(context.Context, bool, int) ([]TagDTO, error) { return nil, nil }
func (l failingPhysicalDeleteLibrary) PublicFileID(types.FileInfo) string { return "public" }
func (l failingPhysicalDeleteLibrary) GetFileByPublicID(context.Context, string) (types.FileInfo, error) { return l.file, nil }
func (l failingPhysicalDeleteLibrary) DeleteFileByPublicID(context.Context, string) (bool, error) { return false, errors.New("database write failed") }
''')

              def replace(path, old, new):
                  p = Path(path)
                  text = p.read_text()
                  if old not in text:
                      raise SystemExit(f'expected text not found in {path}: {old[:100]!r}')
                  p.write_text(text.replace(old, new, 1))

              replace('internal/serve/browse.go',
                  '    MediaURLs       MediaURLs     `json:"media_urls"`\n',
                  '    MediaURLs       MediaURLs     `json:"media_urls"`\n    CanDelete       bool          `json:"can_delete"`\n')
              old_delete = '''func (s *Server) handleDeleteFile(w http.ResponseWriter, r *http.Request, publicID string) {
\tvar req struct {
\t\tMode string `json:"mode"`
\t}
\tif r.Body != nil && r.ContentLength != 0 {
\t\tif err := json.NewDecoder(r.Body).Decode(&req); err != nil {
\t\t\twriteError(w, http.StatusBadRequest, "invalid_request", "request body must be valid JSON", nil)
\t\t\treturn
\t\t}
\t}
\tif req.Mode == "" {
\t\treq.Mode = "untrack"
\t}
\tif req.Mode != "untrack" {
\t\twriteError(w, http.StatusBadRequest, "invalid_request", "mode must be untrack", nil)
\t\treturn
\t}
\tif _, ok := s.library.(PublicFileLibrary); !ok {
\t\twriteError(w, http.StatusServiceUnavailable, "service_unavailable", "file mutation service is not configured", nil)
\t\treturn
\t}
\tdeleted, err := s.deleteFileByPublicID(r.Context(), publicID)
\tif errors.Is(err, ErrNotFound) {
\t\twriteError(w, http.StatusNotFound, "not_found", "file not found", nil)
\t\treturn
\t}
\tif err != nil {
\t\twriteError(w, http.StatusInternalServerError, "internal_error", "failed to untrack file", nil)
\t\treturn
\t}
\tif !deleted {
\t\twriteError(w, http.StatusNotFound, "not_found", "file not found", nil)
\t\treturn
\t}
\twriteJSON(w, http.StatusOK, map[string]interface{}{"mode": "untrack", "removed_locations": 1})
}'''
              new_delete = '''func (s *Server) handleDeleteFile(w http.ResponseWriter, r *http.Request, publicID string) {
\tvar req struct {
\t\tMode string `json:"mode"`
\t}
\tif r.Body != nil && r.ContentLength != 0 {
\t\tif err := json.NewDecoder(r.Body).Decode(&req); err != nil {
\t\t\twriteError(w, http.StatusBadRequest, "invalid_request", "request body must be valid JSON", nil)
\t\t\treturn
\t\t}
\t}
\tif req.Mode == "" {
\t\treq.Mode = "untrack"
\t}
\tif req.Mode != "untrack" && req.Mode != "delete" {
\t\twriteError(w, http.StatusBadRequest, "invalid_request", "mode must be untrack or delete", nil)
\t\treturn
\t}
\tif _, ok := s.library.(PublicFileLibrary); !ok {
\t\twriteError(w, http.StatusServiceUnavailable, "service_unavailable", "file mutation service is not configured", nil)
\t\treturn
\t}

\tvar deleted bool
\tvar err error
\tif req.Mode == "delete" {
\t\tdeleted, err = s.deleteManagedFile(r.Context(), publicID)
\t} else {
\t\tdeleted, err = s.deleteFileByPublicID(r.Context(), publicID)
\t}
\tif errors.Is(err, ErrNotFound) {
\t\twriteError(w, http.StatusNotFound, "not_found", "file not found", nil)
\t\treturn
\t}
\tif errors.Is(err, ErrFileNotManaged) {
\t\twriteError(w, http.StatusConflict, "file_not_managed", "file is outside configured upload targets; untrack it instead", nil)
\t\treturn
\t}
\tif err != nil {
\t\taction := "untrack"
\t\tif req.Mode == "delete" {
\t\t\taction = "delete"
\t\t}
\t\twriteError(w, http.StatusInternalServerError, "internal_error", "failed to "+action+" file", nil)
\t\treturn
\t}
\tif !deleted {
\t\twriteError(w, http.StatusNotFound, "not_found", "file not found", nil)
\t\treturn
\t}
\twriteJSON(w, http.StatusOK, map[string]interface{}{"mode": req.Mode, "removed_locations": 1})
}'''
              replace('internal/serve/browse.go', old_delete, new_delete)
              replace('internal/serve/browse.go',
                  '\t\tMediaURLs: MediaURLs{\n\t\t\tThumbnail: "/api/v1/files/" + id + "/thumbnail",\n\t\t\tPreview:   "/api/v1/files/" + id + "/preview",\n\t\t\tContent:   "/api/v1/files/" + id + "/content",\n\t\t\tDownload:  "/api/v1/files/" + id + "/download",\n\t\t},\n',
                  '\t\tMediaURLs: MediaURLs{\n\t\t\tThumbnail: "/api/v1/files/" + id + "/thumbnail",\n\t\t\tPreview:   "/api/v1/files/" + id + "/preview",\n\t\t\tContent:   "/api/v1/files/" + id + "/content",\n\t\t\tDownload:  "/api/v1/files/" + id + "/download",\n\t\t},\n\t\tCanDelete: s.canDeleteFilePath(file.Path),\n')

              replace('docs/openapi.yaml',
                  '      summary: Untrack one file location.\n      description: Removes the selected location from the library database. Content and tags remain when other locations reference the same content.\n',
                  '      summary: Untrack a file location or delete managed uploaded content.\n      description: Untrack removes only the selected database location. Delete also removes the underlying file, but is accepted only for paths inside configured upload targets.\n')
              replace('docs/openapi.yaml', '                  enum: [untrack]\n                  default: untrack\n', '                  enum: [untrack, delete]\n                  default: untrack\n')
              replace('docs/openapi.yaml',
                  '        "404":\n          $ref: "#/components/responses/NotFound"\n  /files/{id}/thumbnail:',
                  '        "404":\n          $ref: "#/components/responses/NotFound"\n        "409":\n          description: Physical deletion was requested for a file outside configured upload targets.\n          content:\n            application/json:\n              schema:\n                $ref: "#/components/schemas/ErrorResponse"\n  /files/{id}/thumbnail:')
              replace('docs/openapi.yaml',
                  '        - media_urls\n      properties:\n',
                  '        - media_urls\n        - can_delete\n      properties:\n')
              replace('docs/openapi.yaml',
                  '        media_urls:\n          $ref: "#/components/schemas/MediaURLs"\n',
                  '        media_urls:\n          $ref: "#/components/schemas/MediaURLs"\n        can_delete:\n          type: boolean\n          description: True when the file path is inside a configured upload target and may be physically deleted through the API.\n')
              replace('docs/openapi.yaml', '          enum: [untrack]\n        removed_locations:', '          enum: [untrack, delete]\n        removed_locations:')

              replace('docs/SERVE.md',
                  '`DELETE /api/v1/files/{id}` currently supports `{"mode":"untrack"}` to remove a\ntracked location without deleting the underlying file from disk.\n',
                  '`DELETE /api/v1/files/{id}` accepts `{"mode":"untrack"}` to remove only the\ntracked location or `{"mode":"delete"}` to also remove the underlying file. Physical\ndeletion is deliberately limited to files inside configured upload-target roots; other\nindexed paths remain untrack-only. File responses expose `can_delete` so clients do not\noffer destructive deletion where the server will refuse it.\n')

              replace('frontend/src/lib/api/client.ts',
                  '  async untrackFile(id: string): Promise<void> {\n    await this.unwrap(\n      this.client.DELETE(\'/files/{id}\', {\n        params: { header: this.csrfHeaderParam(\'DELETE\'), path: { id } },\n        body: { mode: \'untrack\' }\n      })\n    );\n  }\n',
                  '  async removeFile(id: string, mode: \'untrack\' | \'delete\' = \'untrack\'): Promise<void> {\n    await this.unwrap(\n      this.client.DELETE(\'/files/{id}\', {\n        params: { header: this.csrfHeaderParam(\'DELETE\'), path: { id } },\n        body: { mode }\n      })\n    );\n  }\n')
              replace('frontend/src/lib/queries/files.ts',
                  'export function createUntrackFileMutation(getCSRFToken: () => string, queryClient: QueryClient) {\n  return createMutation<void, Error, string>(() => ({\n    mutationFn: (id) => new ApiClient(getCSRFToken()).untrackFile(id),\n',
                  'export interface FileRemovalVariables {\n  id: string;\n  mode: \'untrack\' | \'delete\';\n}\n\nexport function createFileRemovalMutation(getCSRFToken: () => string, queryClient: QueryClient) {\n  return createMutation<void, Error, FileRemovalVariables>(() => ({\n    mutationFn: ({ id, mode }) => new ApiClient(getCSRFToken()).removeFile(id, mode),\n')

              app = Path('frontend/src/lib/components/AuthenticatedApp.svelte')
              text = app.read_text()
              text = text.replace('createFilesQuery, createTagMutation, createUntrackFileMutation, pageTokenOffset', 'createFilesQuery, createFileRemovalMutation, createTagMutation, pageTokenOffset')
              text = text.replace("'bulk-filtered' | 'untrack-file';", "'bulk-filtered' | 'untrack-file' | 'delete-file';")
              text = text.replace('const untrackFileMutation = createUntrackFileMutation(() => $authState.csrfToken, queryClient);', 'const fileRemovalMutation = createFileRemovalMutation(() => $authState.csrfToken, queryClient);')
              text = text.replace("  function untrackPreview(file: { id: string; name: string }) {\n    actionDialog = { kind: 'untrack-file', value: '', error: '', busy: false, id: file.id, name: file.name, previousQuery: '' };\n  }\n",
                  "  function untrackPreview(file: { id: string; name: string }) {\n    actionDialog = { kind: 'untrack-file', value: '', error: '', busy: false, id: file.id, name: file.name, previousQuery: '' };\n  }\n\n  function deletePreview(file: { id: string; name: string }) {\n    actionDialog = { kind: 'delete-file', value: '', error: '', busy: false, id: file.id, name: file.name, previousQuery: '' };\n  }\n")
              text = text.replace("      } else if (actionDialog.kind === 'untrack-file') {\n        await untrackFileMutation.mutateAsync(actionDialog.id);\n        if (library.activeFile?.id === actionDialog.id) library.closePreview();\n      }",
                  "      } else if (actionDialog.kind === 'untrack-file' || actionDialog.kind === 'delete-file') {\n        await fileRemovalMutation.mutateAsync({ id: actionDialog.id, mode: actionDialog.kind === 'delete-file' ? 'delete' : 'untrack' });\n        if (library.activeFile?.id === actionDialog.id) library.closePreview();\n      }")
              text = text.replace("      case 'untrack-file': return 'Remove from library';", "      case 'untrack-file': return 'Remove from library';\n      case 'delete-file': return 'Delete file';")
              text = text.replace("      case 'untrack-file': return `Untrack \\\"${actionDialog.name}\\\" from the library. The file remains on disk.`;",
                  "      case 'untrack-file': return `Untrack \\\"${actionDialog.name}\\\" from the library. The file remains on disk.`;\n      case 'delete-file': return `Permanently delete \\\"${actionDialog.name}\\\" from disk and remove it from the library.`;")
              text = text.replace("      case 'untrack-file': return 'Remove';", "      case 'untrack-file': return 'Remove';\n      case 'delete-file': return 'Delete file';")
              text = text.replace('      onUntrack={untrackPreview}\n', '      onUntrack={untrackPreview}\n      onDelete={deletePreview}\n')
              text = text.replace("destructive={actionDialog.kind === 'save-delete' || actionDialog.kind === 'untrack-file'}", "destructive={actionDialog.kind === 'save-delete' || actionDialog.kind === 'untrack-file' || actionDialog.kind === 'delete-file'}")
              text = text.replace("input={actionDialog.kind !== 'save-delete' && actionDialog.kind !== 'untrack-file'}", "input={actionDialog.kind !== 'save-delete' && actionDialog.kind !== 'untrack-file' && actionDialog.kind !== 'delete-file'}")
              app.write_text(text)

              preview = Path('frontend/src/lib/components/PreviewDialog.svelte')
              text = preview.read_text()
              text = text.replace('    onTagSearch,\n    onUntrack\n', '    onTagSearch,\n    onUntrack,\n    onDelete\n')
              text = text.replace('    onUntrack: (file: FileItem) => void;\n', '    onUntrack: (file: FileItem) => void;\n    onDelete: (file: FileItem) => void;\n')
              text = text.replace('    <button class="g-btn g-btn-ghost" type="button" title="Remove from library" aria-label={`Remove ${file.name} from library`} onclick={() => onUntrack(file)}><Icon name="trash" size={16} /></button>\n',
                  '    {#if file.can_delete}\n      <button class="g-btn g-btn-ghost" type="button" title="Delete file from disk" aria-label={`Delete ${file.name} from disk`} onclick={() => onDelete(file)}><Icon name="trash" size={16} /></button>\n    {/if}\n    <button class="g-btn g-btn-ghost" type="button" title="Remove from library without deleting the file" aria-label={`Untrack ${file.name} from library`} onclick={() => onUntrack(file)}><Icon name="close" size={16} /></button>\n')
              preview.write_text(text)
          PY

          gofmt -w internal/serve/file_delete.go internal/serve/file_delete_test.go internal/serve/browse.go
          cd frontend
          npm ci
          npm run generate:api
          cd ..

          if ! git diff --quiet; then
            git config user.name "autonomous-maintainer"
            git config user.email "64778819+fiso64@users.noreply.github.com"
            git add internal/serve/file_delete.go internal/serve/file_delete_test.go internal/serve/browse.go docs/openapi.yaml docs/SERVE.md frontend/src/lib/api/client.ts frontend/src/lib/api/openapi.ts frontend/src/lib/queries/files.ts frontend/src/lib/components/AuthenticatedApp.svelte frontend/src/lib/components/PreviewDialog.svelte
            git commit -m "feat(files): safely delete managed uploaded files"
            git push origin HEAD:feat/21-delete-uploaded-files
          fi
      - name: Go build
        run: go build ./...
      - name: Go tests
        run: go test ./...
      - name: Frontend unit tests
        working-directory: frontend
        run: npm run test:unit
      - name: Frontend check
        working-directory: frontend
        run: npm run check
      - name: Frontend build
        working-directory: frontend
        run: npm run build
