name: Maintainer issue 656 helper

on:
  push:
    branches:
      - maintainer/issue-656-untrack-external-delete

permissions:
  contents: write

jobs:
  helper:
    runs-on: [self-hosted, gooru]
    steps:
      - uses: actions/checkout@v4
      - name: Verify helper runner
        run: echo ready
