name: temporary 549 delete obsolete audit branch

on:
  push:
    branches:
      - maintainer/549-batch-disassociate-tags-query

permissions:
  contents: write

jobs:
  cleanup:
    runs-on: [self-hosted, gooru]
    steps:
      - uses: actions/checkout@v4
      - name: Delete obsolete audit branch
        shell: bash
        run: |
          set -euo pipefail
          git push origin --delete maintainer/549-db-helper-caller-audit
      - name: Remove temporary workflow
        shell: bash
        run: |
          set -euo pipefail
          rm .github/workflows/temporary-549-delete-obsolete-audit-branch.yml
          git config user.name "chatgpt-codex-connector"
          git config user.email "64778819+fiso64@users.noreply.github.com"
          git add -A
          git commit -m "chore: remove temporary audit cleanup workflow"
          git push origin HEAD:maintainer/549-batch-disassociate-tags-query
