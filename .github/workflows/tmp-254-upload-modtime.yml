name: tmp-254-upload-modtime

on:
  push:
    branches:
      - feat/254-preserve-upload-modtime

permissions:
  contents: write

jobs:
  patch-and-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Apply product patch
        run: python .github/tmp-254-patch.py
      - name: Format Go
        run: gofmt -w internal/serve/config.go internal/serve/uploads.go internal/serve/upload_stream.go internal/serve/upload_modtime_test.go internal/serve/config_test.go
      - name: Backend validation
        run: go test ./internal/serve
      - name: Frontend validation
        working-directory: frontend
        run: |
          npm ci
          npm run check
          npm run test:unit
          npm run build
      - name: Diff check
        run: git diff --check
      - name: Commit product patch if needed
        run: |
          git config user.name 'chatgpt-codex-connector'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add internal/serve frontend/src/lib/api/client.ts docs
          if ! git diff --cached --quiet; then
            git commit -m 'feat(upload): preserve source modification times'
            git push origin HEAD:feat/254-preserve-upload-modtime
          fi
