name: Temporary #549 validation

on:
  push:
    branches:
      - maintainer-549-share-tag-filter-query

permissions:
  contents: read

jobs:
  validate:
    runs-on: [self-hosted, gooru]
    env:
      GOTOOLCHAIN: local
    steps:
      - uses: actions/checkout@v4
      - name: Verify formatting
        run: |
          gofmt -w internal/database/database.go
          git diff --exit-code -- internal/database/database.go
      - name: Test
        run: go test -count=1 ./...
      - name: Build
        run: go build ./...
