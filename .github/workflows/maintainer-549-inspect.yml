name: Temporary #549 validation

on:
  push:
    branches:
      - maintainer-549-share-tag-filter-query

permissions:
  contents: read

jobs:
  validate:
    runs-on: [self-hosted, gooru]
    env:
      GOTOOLCHAIN: local
    steps:
      - uses: actions/checkout@v4
      - name: Test
        run: go test -count=1 ./...
      - name: Build
        run: go build ./...
