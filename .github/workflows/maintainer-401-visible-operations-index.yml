name: Maintainer 401 visible operations index

on:
  push:
    branches:
      - maintainer/401-visible-operations-index
    paths:
      - .github/workflows/maintainer-401-visible-operations-index.yml

jobs:
  validate:
    runs-on: [self-hosted, gooru]
    steps:
      - uses: actions/checkout@v4
      - name: Focused database tests
        run: go test ./internal/database
      - name: Full Go tests
        run: go test ./...
