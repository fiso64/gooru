name: Temporary #248 validation

on:
  push:
    branches:
      - fix/248-upload-target-default-tag-regressions

permissions:
  contents: read

jobs:
  focused:
    runs-on: [self-hosted, gooru]
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install dependencies
        run: npm ci --prefer-offline --no-audit
      - name: Frontend check
        id: check
        continue-on-error: true
        run: npm run check > ../tmp-248-check.log 2>&1
      - name: Upload check diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: tmp-248-check-log
          path: tmp-248-check.log
      - name: Fail on frontend check
        if: steps.check.outcome == 'failure'
        run: exit 1
      - name: Upload workflow unit tests
        run: npm run test:unit -- uploadWorkflow.svelte.test.ts
      - name: Focused upload browser regression
        run: npm run test:e2e -- tests/upload-drop-control.spec.ts --project=chromium
