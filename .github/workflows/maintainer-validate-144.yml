name: maintainer-validate-144
on:
  push:
    branches:
      - fix/102-job-lifecycle-logging
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - run: go test ./internal/serve
