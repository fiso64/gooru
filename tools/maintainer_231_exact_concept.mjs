import fs from 'node:fs';

const viewerPath = 'frontend/src/lib/components/ViewerStage.svelte';
let viewer = fs.readFileSync(viewerPath, 'utf8');

const stateNeedle = `  let stageElement = $state<HTMLDivElement | undefined>();\n`;
if (!viewer.includes(stateNeedle)) throw new Error('ViewerStage state anchor missing');
viewer = viewer.replace(stateNeedle, `${stateNeedle}  let comicTransition = $state<'' | 'entering' | 'exiting'>('');\n  let previousComicEntered = comicEntered;\n`);

const effectNeedle = `  $effect(() => {\n    if (comicEntered) showPlaybackControls();\n  });\n`;
if (!viewer.includes(effectNeedle)) throw new Error('comic controls effect anchor missing');
viewer = viewer.replace(effectNeedle, `${effectNeedle}\n  $effect(() => {\n    const nextComicEntered = comicEntered;\n    if (nextComicEntered === previousComicEntered) return;\n    previousComicEntered = nextComicEntered;\n    comicTransition = nextComicEntered ? 'entering' : 'exiting';\n    const timer = setTimeout(() => { comicTransition = ''; }, nextComicEntered ? 520 : 460);\n    return () => clearTimeout(timer);\n  });\n`);

const stageNeedle = `<div bind:this={stageElement} class:fullscreen={isFullscreen} class:waiting={waitingForTarget} class:cursor-idle={isFullscreen && cursorIdle} class:comic-reading={comicEntered} class="lightbox-stage viewer-stage" tabindex="-1" aria-busy={waitingForTarget} onpointermove={handleStagePointerMove}>`;
const stageReplacement = `<div bind:this={stageElement} class:fullscreen={isFullscreen} class:waiting={waitingForTarget} class:cursor-idle={isFullscreen && cursorIdle} class:comic-reading={comicEntered} class:entering={comicTransition === 'entering'} class:exiting={comicTransition === 'exiting'} class="lightbox-stage viewer-stage" tabindex="-1" aria-busy={waitingForTarget} onpointermove={handleStagePointerMove}>`;
if (!viewer.includes(stageNeedle)) throw new Error('viewer stage markup anchor missing');
viewer = viewer.replace(stageNeedle, stageReplacement);

const buttonNeedle = `  {#if comicAvailable && !comicEntered}\n    <button class="comic-read-button" type="button" style={comicReadStyle} disabled={comicLoading} aria-label="Read comic" onclick={(event) => { onToggleComic?.(); restoreStageFocusAfterPointer(event); }}>\n      <span class="comic-read-arrow comic-read-arrow-left" aria-hidden="true">→</span>\n      <span>{comicLoading ? 'Loading comic…' : 'Read comic'}</span>\n      <span class="comic-read-arrow comic-read-arrow-right" aria-hidden="true">←</span>\n    </button>\n    {#if comicError}<div class="comic-error-overlay" role="alert">{comicError}</div>{/if}\n  {/if}\n`;
const buttonReplacement = `  {#if comicAvailable && !comicEntered}\n    <button class="comic-read-button" type="button" style={comicReadStyle} disabled={comicLoading} aria-label="Read comic" onclick={(event) => { onToggleComic?.(); restoreStageFocusAfterPointer(event); }}>\n      <svg class="icon" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.55" aria-hidden="true">\n        <path d="M4.2 3.8h7.1a2 2 0 0 1 2 2v10.4H6.2a2 2 0 0 1-2-2V3.8Z"/>\n        <path d="M13.3 5.8h2.5v10.4h-7a2.6 2.6 0 0 0-2.6 0"/>\n      </svg>\n      <span>{comicLoading ? 'Loading comic…' : 'Read comic'}</span>\n      <svg class="icon enter-arrow" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.55" aria-hidden="true">\n        <path d="M4 10h11M11 6l4 4-4 4"/>\n      </svg>\n    </button>\n    {#if comicError}<div class="comic-error-overlay" role="alert">{comicError}</div>{/if}\n  {/if}\n\n  <div class="transition-arrow" aria-hidden="true"></div>\n`;
if (!viewer.includes(buttonNeedle)) throw new Error('comic read button markup anchor missing');
viewer = viewer.replace(buttonNeedle, buttonReplacement);
fs.writeFileSync(viewerPath, viewer);

const cssPath = 'frontend/src/lib/styles/viewer.css';
let css = fs.readFileSync(cssPath, 'utf8');
const start = css.indexOf(`/* Copy the supplied concept's read-cover-button treatment verbatim; only its`);
const end = css.indexOf('/* Keep rotation/fade transitions, but do not tween media dimensions between files. */');
if (start < 0 || end < 0 || end <= start) throw new Error('viewer.css concept block anchors missing');
const exactBlock = `/* The supplied concept's read-cover-button treatment; placement is the issue's\n   explicit top-right-of-rendered-image deviation. */\n.comic-read-button {\n  position: absolute;\n  z-index: 8;\n  top: 48px;\n  right: 48px;\n  display: inline-flex;\n  align-items: center;\n  gap: 9px;\n  height: 42px;\n  padding: 0 15px 0 13px;\n  border-radius: 8px;\n  border: 1px solid rgba(255,255,255,.24);\n  background: rgba(16,16,14,.78);\n  color: #fff;\n  backdrop-filter: blur(12px);\n  box-shadow: 0 10px 34px rgba(0,0,0,.34);\n  cursor: pointer;\n  font-size: 13px;\n  font-weight: 600;\n  letter-spacing: .005em;\n  transition:\n    background 140ms ease,\n    border-color 140ms ease,\n    color 140ms ease,\n    transform 180ms cubic-bezier(.2,.78,.22,1),\n    box-shadow 180ms ease;\n}\n\n.comic-read-button:hover,\n.comic-read-button:focus-visible {\n  background: var(--accent);\n  border-color: var(--accent);\n  color: var(--accent-ink);\n  transform: translateY(-2px) scale(1.015);\n  box-shadow: 0 14px 38px rgba(0,0,0,.44);\n  outline: none;\n}\n\n.comic-read-button:disabled {\n  opacity: .65;\n  cursor: wait;\n}\n\n.comic-read-button .icon {\n  width: 16px;\n  height: 16px;\n  display: block;\n  flex: 0 0 auto;\n}\n\n.comic-read-button .icon path {\n  vector-effect: non-scaling-stroke;\n}\n\n.comic-read-button .enter-arrow {\n  transition: transform 180ms cubic-bezier(.2,.78,.22,1);\n}\n.comic-read-button:hover .enter-arrow { transform: translateX(3px); }\n\n/* Directional transition accent copied structurally and numerically from the\n   supplied concept. The arrowhead is the element's own ::after child so it\n   inherits the exact parent transform/keyframes. */\n.transition-arrow {\n  position: absolute;\n  z-index: 7;\n  left: 50%;\n  top: 50%;\n  width: 138px;\n  height: 1px;\n  background: linear-gradient(90deg, transparent, rgba(246,222,119,.82), transparent);\n  transform: translate(-50%,-50%) scaleX(.05);\n  opacity: 0;\n  pointer-events: none;\n}\n.transition-arrow::after {\n  content: "";\n  position: absolute;\n  right: 18px;\n  top: 50%;\n  width: 8px;\n  height: 8px;\n  border-top: 1.5px solid var(--accent);\n  border-right: 1.5px solid var(--accent);\n  transform: translateY(-50%) rotate(45deg);\n}\n\n/* Gooru derives the real image geometry rather than imposing the concept's\n   synthetic fixed frame, but preserves its 61/52 growth and 420ms easing. */\n.viewer-stage.entering .viewer-pan-surface {\n  animation: comic-frame-enter 420ms cubic-bezier(.2,.78,.22,1) both;\n}\n.viewer-stage.exiting .viewer-pan-surface {\n  animation: comic-frame-exit 420ms cubic-bezier(.2,.78,.22,1) both;\n}\n.viewer-stage.comic-reading:not(.entering) .viewer-pan-surface {\n  transform: scale(1.1730769231);\n}\n\n.viewer-stage.entering .transition-arrow {\n  animation: arrow-through 460ms cubic-bezier(.2,.78,.22,1);\n}\n@keyframes arrow-through {\n  0% { opacity: 0; transform: translate(-50%,-50%) scaleX(.05); }\n  24% { opacity: .95; }\n  65% { opacity: .8; transform: translate(-50%,-50%) scaleX(1); }\n  100% { opacity: 0; transform: translate(18%,-50%) scaleX(.45); }\n}\n\n.viewer-stage.exiting .transition-arrow {\n  transform: translate(-50%,-50%) rotate(180deg);\n  animation: arrow-back 400ms cubic-bezier(.2,.78,.22,1);\n}\n@keyframes arrow-back {\n  0% { opacity: 0; transform: translate(-50%,-50%) rotate(180deg) scaleX(.08); }\n  30% { opacity: .9; }\n  72% { opacity: .65; transform: translate(-50%,-50%) rotate(180deg) scaleX(.92); }\n  100% { opacity: 0; transform: translate(-115%,-50%) rotate(180deg) scaleX(.38); }\n}\n\n@keyframes comic-frame-enter {\n  from { transform: scale(1); }\n  to { transform: scale(1.1730769231); }\n}\n@keyframes comic-frame-exit {\n  from { transform: scale(1.1730769231); }\n  to { transform: scale(1); }\n}\n\n.comic-error-overlay {\n  position: absolute;\n  z-index: 8;\n  top: 88px;\n  right: 48px;\n  max-width: min(360px, calc(100% - 96px));\n  padding: 7px 10px;\n  border-radius: 4px;\n  background: rgba(0, 0, 0, .72);\n  color: var(--danger);\n  font-size: 12px;\n}\n\n.lightbox-video-controls.comic-controls {\n  animation: comic-controls-enter 220ms cubic-bezier(.2,.78,.22,1);\n}\n.comic-exit { gap: 5px; min-width: max-content; }\n.comic-exit span {\n  font-size: 9px;\n  opacity: .72;\n  text-transform: uppercase;\n  letter-spacing: .08em;\n}\n@keyframes comic-controls-enter {\n  from { opacity: 0; transform: translate(-50%, 8px); }\n  to { opacity: 1; transform: translate(-50%, 0); }\n}\n\n@media (prefers-reduced-motion: reduce) {\n  .comic-read-button,\n  .comic-read-button .enter-arrow,\n  .lightbox-video-controls.comic-controls,\n  .viewer-stage .viewer-pan-surface,\n  .transition-arrow {\n    animation-duration: 1ms !important;\n    animation-iteration-count: 1 !important;\n    transition-duration: 1ms !important;\n  }\n}\n\n`;
css = css.slice(0, start) + exactBlock + css.slice(end);
fs.writeFileSync(cssPath, css);

const testPath = 'frontend/tests/comic-ui.spec.ts';
let test = fs.readFileSync(testPath, 'utf8');
const oldGlyph = `  await expect(readComic.locator('.comic-read-arrow-left')).toHaveCSS('width', '16px');\n  await expect(readComic.locator('.comic-read-arrow-right')).toHaveCSS('width', '16px');\n  await expect(readComic.locator('.comic-read-arrow-left')).toHaveCSS('background-image', /svg/);\n  await expect(readComic.locator('.comic-read-arrow-right')).toHaveCSS('background-image', /svg/);\n`;
const newGlyph = `  const bookIcon = readComic.locator('svg.icon').first();\n  const enterIcon = readComic.locator('svg.enter-arrow');\n  await expect(bookIcon).toHaveAttribute('stroke', 'currentColor');\n  await expect(bookIcon).toHaveAttribute('stroke-width', '1.55');\n  await expect(bookIcon.locator('path').first()).toHaveAttribute('d', 'M4.2 3.8h7.1a2 2 0 0 1 2 2v10.4H6.2a2 2 0 0 1-2-2V3.8Z');\n  await expect(enterIcon).toHaveAttribute('stroke', 'currentColor');\n  await expect(enterIcon.locator('path')).toHaveAttribute('d', 'M4 10h11M11 6l4 4-4 4');\n`;
if (!test.includes(oldGlyph)) throw new Error('comic glyph test anchor missing');
test = test.replace(oldGlyph, newGlyph);

test = test.replace(`  await expect.poll(() => stage.evaluate((element) => getComputedStyle(element, '::after').animationName)).toBe('arrow-through');\n  await expect.poll(() => stage.evaluate((element) => getComputedStyle(element, '::after').animationDuration)).toBe('0.46s');\n`, `  const transitionArrow = stage.locator('.transition-arrow');\n  await expect(stage).toHaveClass(/entering/);\n  await expect(transitionArrow).toHaveCount(1);\n  await expect.poll(() => transitionArrow.evaluate((element) => getComputedStyle(element).animationName)).toBe('arrow-through');\n  await expect.poll(() => transitionArrow.evaluate((element) => getComputedStyle(element).animationDuration)).toBe('0.46s');\n  await expect.poll(() => transitionArrow.evaluate((element) => getComputedStyle(element, '::after').content)).toBe('""');\n`);

test = test.replace(`  await expect.poll(() => stage.evaluate((element) => getComputedStyle(element, '::after').animationName)).toBe('arrow-back');\n  await expect.poll(() => stage.evaluate((element) => getComputedStyle(element, '::after').animationDuration)).toBe('0.4s');\n`, `  await expect(stage).toHaveClass(/exiting/);\n  await expect.poll(() => transitionArrow.evaluate((element) => getComputedStyle(element).animationName)).toBe('arrow-back');\n  await expect.poll(() => transitionArrow.evaluate((element) => getComputedStyle(element).animationDuration)).toBe('0.4s');\n`);

test = test.replace(`  await expect(stage.locator('.viewer-pan-surface')).toHaveCSS('animation-duration', '0.001s');\n`, `  await expect(stage.locator('.viewer-pan-surface')).toHaveCSS('animation-duration', '0.001s');\n  await expect(stage.locator('.transition-arrow')).toHaveCSS('animation-duration', '0.001s');\n`);
fs.writeFileSync(testPath, test);
